version: '3.9'
services:
  api:
    image: ghcr.io/<owner>/roomrental-backend:staging
    env_file:
      - ../backend/.env.staging
    ports:
      - "4100:4100"
    restart: unless-stopped

  worker:
    image: ghcr.io/<owner>/roomrental-backend:staging
    env_file:
      - ../backend/.env.staging
    command: ["node", "src/workers/index.js"]
    restart: unless-stopped
